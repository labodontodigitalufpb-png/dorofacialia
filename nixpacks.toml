providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_PYTHON_PACKAGE_MANAGER = "requirements"

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080} --proxy-headers"
